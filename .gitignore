__pycache__/
*.pyc

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.rollup.cache/
*.tsbuildinfo
htmlcov/
.coverage
.coverage.*
coverage.xml

# Local working context, not in-repo
CLAUDE.md
PLAN.md

# Local virtualenvs
.venv/
venv/

# JS toolchain
node_modules/

# NOTE: the built bundle at custom_components/teslamate_cards/frontend/ is
# deliberately COMMITTED -- HACS installs straight from the repository, so a
# gitignored bundle means a HACS install ships no cards at all.
